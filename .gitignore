node_modules/
.next/
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
supabase/.temp/
.vercel

# Vendor SPA build output (generated at build time into the Next app)
/public/vendor
/public/app

# Graphify local cache, dated backups, and machine-specific metadata
graphify-out/cache/
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
graphify-out/.graphify_root
graphify-out/.graphify_python
graphify-out/.graphify_labels.json

# Generated email preview gallery (large, self-contained)
email-previews/

# Local demo/test credentials — never commit
.demo-credentials.local.md

# Vendor pipeline artefacts: the raw Apify crawl and its structured output.
# Intermediate only — the real data lands in Supabase via import-vendors.mjs.
# Third-party scraped content, so it stays out of the repo.
# Leading slash matters: an unanchored `data/` would also swallow
# frontends/parent/src/data/, which is source.
/data/

# macOS finder metadata.
.DS_Store
**/.DS_Store
